local = "Size"
local = "text"
repeat.local watermark - true
local = "watermark"
local = false
{watermark} - false
screen.Gui.Api.Renderwindow - true
Name = "clumsy"
function = function(callback)
if callback then
watermark = true
Create.watermark.ScreenGui.Api
task.wait = (0.3)
repeat.task.wait - (0.3)
local = false
normal = {
watermark - Color3
RBGcolor = false
execute - true
--text size LMAO
Size = {1.3, 0.5, 0.8} = false
text = "clumsy"
({
end)
selectedcolor = {"blue, green, yellow"}
    sendnotification = "the watermark changed into <color>
}}
{{
hovertext = "watermark made by clamsy"
    }
}
end)
